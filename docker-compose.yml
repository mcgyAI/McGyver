version: '3.8'
services:
  mongo:
    image: mongo:7
    container_name: mcgyver-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mcgyver-mongo-data:/data/db

volumes:
  mcgyver-mongo-data:
